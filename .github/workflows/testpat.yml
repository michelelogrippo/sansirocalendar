name: Test PAT Access

on:
  workflow_dispatch:

jobs:
  test-access:
    runs-on: ubuntu-latest
    steps:
      - name: Test access with PAT
        run: |
          git ls-remote https://x-access-token:${{ secrets.GH_PAT }}@github.com/michelelogrippo/sansirocalendar.git
